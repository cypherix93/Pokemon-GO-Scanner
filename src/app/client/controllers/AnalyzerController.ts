import {Controller} from "../base/Controller";
import q = require("q");

export class AnalyzerController extends Controller
{
    constructor()
    {
        super();

        this.initSelf(this);
    }
}